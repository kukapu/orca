# Release v1.4.197-kukapu.2

Sustituida en el VPS el **2026-09-08** por
[v1.4.198-kukapu.1](./v1.4.198-kukapu.1.md). Los apartados siguientes conservan
la evidencia historica de esta version, no el estado vigente del servidor.

El paquete instalado de esta version fue el fechado **2026-09-07**, descrito al
final de esta pagina. Los resultados del 6 de septiembre son antecedentes, no
validacion del paquete posterior a upstream.

## Antecedente 2026-09-06

- **Build**: compilado 2026-09-06 21:10 UTC sobre `ee2e5da315` (`main-kukapu`)
  con arbol de trabajo sucio (fork deltas sin commit; manifiesto de fuentes en
  `docs/features-kukapu/release-delivery-197-2.md`).
- **Empaquetado**: AppImage+deb generados 21:20 UTC en
  `dist/release-1.4.197-kukapu.2/` con gates automaticos verdes (glibc 2.31
  floor, daemon, plugin) y asar limpio verificado. Un primer pack 21:16 UTC fue
  INVALIDADO: el asar arrastro builds previos de `dist/` (output custom dejo de
  excluir el directorio); corregido con exclusiones `!dist` /
  `!{test-results,playwright-report}` en el config y regresion en
  `electron-builder-config.test.mjs`.
- **Estado**: EMPAQUETADA con smoke LOCAL aislado aprobado Y contrato CLI del
  launcher empaquetado verificado por revision independiente (extraido del
  AppImage con sha256 confirmado: `--help` con header `Usage: orca <command>`,
  `--version` EXACTO `1.4.197-kukapu.2`, `status`/`skills --help` exit 0,
  `worktree list` falla limpio sin runtime; serve en display :100 con
  runtimeId/version/dataDir aislados verificados por CLI sin pairing; detalles
  y limites en `docs/features-kukapu/release-delivery-197-2.md`). El smoke
  previo que reporto `v24.18.1`/427 lineas quedo corregido: probo Node por
  via invalida (DISPLAY :99 + forzado externo), no la CLI. Docker cli-contract
  omitido (requiere pull/apt/red no autorizados). NO instalada en produccion;
  aceptada como entrega de candidato, no como autorizacion de instalacion.

## Cambios del fork (sobre v1.4.197-kukapu.1)

- Recuperacion de preguntas tras `agent_prompt_stalled` con capability retenida:
  ask/reply permitidos sin reactivar el Dispatch; supersession (incluye handle
  remintado en el mismo pane) y stop/revoke/abandon bloquean.
- Composer gate de arranque para workers OpenCode nuevos: presupuesto conjunto
  con `tui-idle`, identidad PTY/generacion estable, solo OpenCode.
- Aislamiento del harness de agentes reales: `createElectronHomeIsolation` y
  guard ambiental comparten `agent-runtime-dir-env-keys`; el hijo hereda fuentes
  externas limpias.
- Observed-options y transcripciones de workers (OpenCode/Pi), autoridad de
  lanzamiento por host y pipelines asociados.
- Docs: `docs/uso/` (guias de trabajo), releases y features del fork.

## Validacion 2026-09-06

- Gates de validacion previos (ver `docs/features-kukapu/orchestration-run-197-2.md`):
  smoke GLM real OpenCode+Pi PASSED con aislamiento corregido; suite runtime/
  orquestacion verde; E2E sin LLM revalidado por QA (sim 1/1, web 4/4, real 2).
- Bloque 5: typecheck/relay/CLI/electron-vite/web compilados node-direct;
  precheck nativo `--check-only` bajo Electron exit 0; tools de empaquetado en
  cache. Empaquetado y smoke local del artefacto completados; hashes y
  manifiesto en `dist/release-1.4.197-kukapu.2/` y el documento de entrega.

## Incidencias 2026-09-06

- El primer paquete y la primera comprobacion CLI fueron invalidados y
  sustituidos por las verificaciones descritas arriba. La matriz Docker
  adversarial y la instalacion en produccion siguen pendientes; no se han
  publicado artefactos. Los commits locales de integracion se crearon el dia 7.

## Estado 2026-09-07 (pack fresco)

- Reempaquetado node-direct sobre `42ab555177` en
  `dist/release-1.4.197-kukapu.2-20260907/`: AppImage 195046613 B
  (`4c399be7…a8483`), deb 165434128 B (`ef7b6602…7501c67`), ASAR
  `1.4.197-kukapu.2` limpio, `package.json` SHA-256 identico.
  Detalle y comandos en `docs/features-kukapu/release-delivery-197-2-20260907.md`.
- Smoke local post-upstream aprobado: AppImage/launcher `Usage: orca`, version
  exacta `.2`, runtime privado distinto, metadata coincidente, HTTP 200 y cleanup
  comprobados. Preflight del instalador exit 0, sin mutaciones. Detalles y probes
  invalidados en `docs/features-kukapu/artifact-smoke-197-2-20260907.md`.
- Instalacion EXTERNA terminada por el usuario el 2026-09-07. Verificados CLI y
  VERSION `.2`, ambos servicios activos y runtime
  `b0c74bff-13d3-4ca5-906b-bc361f02372b` `ready`. Hash del AppImage instalado
  identico al candidato fechado. Comandos y rollback en el informe de entrega.
- Codigo y documentacion publicados en `origin/main-kukapu`. Los commits
  documentales posteriores no cambian las fuentes del paquete `42ab555177`.
- Automatizacion diaria existente configurada con `openai/gpt-6-astra`, modelo
  guardado y leido en `.2`; sin lanzar una corrida de prueba. No smoke LLM
  post-upstream ni matriz Docker adversarial.
