# Preparacion 1.4.203-kukapu.1

Fecha: 2026-09-15. Estado: **bloqueada en verificacion**; merge textual resuelto,
artefactos pendientes. No es una release preparada para instalar.

## Alcance y procedencia

El usuario pide integrar todos los cambios de la oficial `.203`, conservar los
cambios propios que sigan teniendo sentido y preparar la nueva instalacion en
`main-kukapu`. Autoriza esta preparacion manual en el checkout actual. Se usa una
rama fuente temporal desde C200 para evitar la ancestry del puente; el resultado
se devolvera a `main-kukapu` por fast-forward tras los gates y el puente local.
La instalacion, push y release remota quedan fuera de esta etapa.

- Base .200 aceptada: [reconciliacion](1.4.200-kukapu.1-reconciliation-20260915.md).
- C200: `11c5c2389c21a7c0382db318c14164f8dc185a49`.
- P200 / F publicado: `9c1d9291725733c90c911cb26ca22ed1d40cc3fd`.
- Arbol C200 = P200: `0279999dd14aeaba19566b41d8fdb4b1f745cc1f`.
- Oficial objetivo `v1.4.203`: `776e424e76405a06851dd9ec9ff3b58ffbdb3eea`.
- Release oficial estable publicada el 2026-09-15; tags peeled verificados.
- Delta oficial .200..203: 259 commits, 2383 paths, +151252/-29245 lineas.
- Sin commits propios posteriores a P200 al inicio. WIP conocido: skill de
  releases, enlaces, gitignore y reconciliacion .200 de esta misma conversacion.
- Inventario actual: un workspace, solo esta sesion; automatizacion del fork
  descubierta por nombre, desactivada y sin runs registrados. El ID del documento
  historico no identifica la definicion actual; no se modifica el scheduler.

## Preservacion y conflictos

Inventario inicial del fork: modelos y opciones observadas OpenCode/Pi,
transcripts/archivo worker, autoridad y persistencia de orquestacion (schema39),
modelo por automatizacion, sesiones y sockets, modelos retirados de source control,
geometria de overlays, parche Linux node-pty, instalador y evidencia de releases.

El merge normal se hizo desde `66bf8c64b5` (C200 mas el procedimiento documental),
contra el OID oficial verificado. Hubo 14 paths con conflicto textual y 49 paths
de solapamiento entre el delta propio y el oficial, incluidos los sin conflicto.

| Area / paths con conflicto | Resolucion |
| --- | --- |
| `package.json`, `resources/skills/release-mapping.json` | Identidad `1.4.203-kukapu.1`; dependencias oficiales .203. |
| `config/electron-builder.config.cjs` | Politica nueva de natives por plataforma y comprobacion glibc; exclusiones privadas del fork conservadas. |
| `src/cli/bundled-skill-guides.ts` | Regenerado desde las guias fusionadas; conservadas aclaraciones OpenCode/Pi. |
| Hook server: `server-state`, `server-types`, `server-status-inference`, `server-status-update` | Nuevo ownership/frescura/notificaciones upstream junto al fencing de sesiones y opciones observadas del fork. |
| `agent-status-handler-source.ts` de Pi | Reutilizada comprobacion upstream del owner (solo ESRCH prueba ausencia); conservados captura de opciones y limites por sesion. |
| `orca-runtime-state-fields.ts` | Conservadas ambas dependencias: sink structured nuevo y lectura de opciones observadas. |
| `automation-schemas.ts` | Reexportacion upstream desde contrato compartido; `model` y `effort` propios portados a `automation-params.ts`. |
| `federation-control.ts`, `federation.ts` | Contratos RPC tipados upstream, preservando gates/settlement propios y extracciones existentes. Descripcion neutral de espera portada al modulo extraido. |
| `StructuredAgentSessionPaneOverlayLayer.tsx` | Geometria medida del fork junto a `isFocusedGroup` de upstream. |

Lockfile contra .203: solo tres sustituciones del hash del parche propio node-pty;
no se han normalizado resoluciones. Electron pasa a 43.7.0 como en upstream.

### Correcciones durante las verificaciones

- Unificado el mock `kill` de Pi, que el merge sin conflicto habia duplicado;
  conservados los dos nombres de entrada usados por las pruebas de ambos lados.
- Ajustado el lookup de un test de worker-stop al nuevo catalogo RPC tipado.
- Corregida la cola de Pi para comprobar la ruta de transcript capturada en el
  evento, en lugar de leer la sesion actual al vaciar una cola retrasada.
- Separadas seleccion y settlement de reservas: el perfil oficial40 utiliza
  sus fases actuales; el perfil fork39 sin migrar conserva su proteccion legacy.
- Admitida recuperacion upstream de stamps anteriores cuando existe la columna
  oficial `home_run_id`; versiones futuras y forma fork39 no reconocida siguen
  rechazadas. La reconciliacion completa de todas las migraciones sigue pendiente.
- Reutilizada la lectura de Delivery acotada a Run/mailbox desde el entry point
  upstream, evitando que la composicion de prototipos anule el filtro del fork.
- Validado el envelope de journals archivados y rechazados kinds desconocidos
  antes de tratarlos como terminal tails.
- Los hooks detectaron dos limites max-lines al guardar el merge: se extrajeron
  las pruebas de orden de flags a `commit-message-opencode-launch-flags.test.ts`
  y la publicacion de evidencia del hook server a `server-status-evidence.ts`.
  No se subieron limites ni se desactivaron reglas. Las 48 pruebas del primer
  split pasan; la comprobacion oxlint de los 2047 archivos JS/TS preparados pasa.

## Verificacion y entrega

| Etapa | Estado |
| --- | --- |
| Inventario y fuente | Revisados; base aceptada y objetos Git comprobados |
| Integracion y conflictos | Merge textual resuelto sobre la rama fuente temporal |
| Typecheck | `pnpm tc`, exit 0 en la tercera comprobacion |
| Tests de areas afectadas | 3898 pasan, 57 fallan, 15 omitidos; 20 suites fallan, 346 pasan, 1 omitida |
| Calidad contra C200 | Bloqueada; ultima ejecucion: 751 findings, 745 en archivos identicos a upstream .203 |
| Guias / catalogo RPC / manifiesto skills | Generadores `--check` y verificador de manifiesto, exit 0 |
| Fuente y version | Candidato `1.4.203-kukapu.1`; no tag de fuente verificada |
| Build / pack / glibc / ASAR | No ejecutados: gates de fuente bloqueados |
| Smoke artefactos / coexistencia | No ejecutado |
| Puente y main-kukapu local | Pendiente de gates; no crear puente antes de aprobarlos |
| Push / draft / manifiesto remoto | No solicitado |
| Instalacion | No solicitada |

## Comandos y evidencia

Entorno de pruebas privado con Node 24.11.1, pnpm 12.0.0, `CI=1`,
`ORCA_BACKGROUND_LAUNCH=1`, HOME/XDG aislados y ocho workers de Vitest. Procesos
lanzados en segundo plano; no se lanzo una app visible ni se accedio a DB real.
Logs y estados de procesos conservados fuera del indice en `.tmp/release-203/logs/`.

La instalacion congelada termino exit 0 (`install-6`). El launcher pnpm de mise
intentaba resolver el packageManager en el HOME privado a un placeholder sin
binario; se uso directamente la instalacion pnpm 12 ya existente. No se modifico
configuracion global ni se omitieron scripts de instalacion.

```text
pnpm install --frozen-lockfile
pnpm tc
pnpm test src/main/agent-hooks src/main/pi src/main/opencode src/main/runtime/orchestration src/main/runtime/rpc/methods/orchestration src/shared/automation-launch-preferences.test.ts src/main/persistence-automations.test.ts src/shared/rpc-contract src/shared/agent-status-types.test.ts src/shared/tui-agent-startup src/shared/commit-message src/shared/source-control-ai.test.ts src/renderer/src/components/tab-group/overlay-slot-geometry.test.ts src/renderer/src/components/tab-group/use-overlay-slot-geometry.test.tsx --maxWorkers=8
pnpm run check:code-quality:changed v1.4.200-kukapu.1
node config/scripts/generate-bundled-skill-guides.mjs --check
node config/scripts/generate-rpc-params-catalog.mjs --check
node config/scripts/generate-skill-bundle-manifest.mjs
git diff --check
```

El check de whitespace del diff completo incluye avisos en capturas PTY oficiales
(CR/ANSI preservados) y lineas de contexto de patches. Se conservan esos bytes;
el check de cambios de codigo editados no detecto marcadores de conflicto.

Tres pasadas de tests: 174 -> 66 -> 57 fallos. Se alcanza el limite de rondas del
procedimiento; conservar el candidato y revisar los siguientes bloques antes de
otra oleada. El gate de calidad permanece fallido: clasificar una incidencia como
heredada no equivale a aprobarla ni justifica desactivar reglas.

## Bloques pendientes para retomar

1. Migraciones/admision de perfiles29/38/39: conciliar los contratos fisicos del
   fork con los casos oficiales de version-skew, sin degradar stamps ni perder facts.
2. Recuperacion y cierre de workers structured historicos frente a workers nuevos;
   lectura de legacy transcript pins y separacion archivo/liveness en RPC.
3. Actualizar fixtures afectados por los contratos nuevos (Run obligatorio, snapshot
   de estado fleet, deteccion de disponibilidad y compositor), preservando sus oraculos.
4. Revisar la compilacion repetida de `PRAGMA table_info(dispatch_contexts)` y el
   fallo de subprocess CLI, que usa un build previo a esta integracion.
5. Resolver el gate de calidad: 748 findings de type assertions y 3 type-aware en
   la ultima pasada. 745 corresponden a archivos byte-identicos a .203; los otros
   seis estan en hook-server y message-inbox compartidos con cambios propios y
   requieren revision. Se conservan ambas ejecuciones y su clasificacion.
6. Verificar delta adicional (cloud, wire y clientes), construir desde fuente limpia,
   probar artefactos y solo entonces cerrar el puente local y main-kukapu.

La .200 en ejecucion sigue siendo la base operativa aceptada. No se ha instalado,
publicado ni generado un manifiesto de entrega terminada para .203.
