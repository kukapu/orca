# Release v1.4.194-kukapu.1

- **Build**: 2026-09-01 ~13:57 UTC · **Instalada**: 2026-09-01 14:24 UTC (backup `orca-linux.AppImage.20260901T142449Z`)
- **Base**: `upstream/main` (`5aa02ead59`), posterior al tag upstream `v1.4.194`. Primera release del fork: la rama `main-kukapu` se creó ese mismo día desde `upstream/main`.
- **Delta del fork**: 9 commits (`5aa02ead59..152fe21012`).

## Cambios del fork

- feat(orchestration): soporte de launch models para workers OpenCode (`--model` opaco; sin `--effort`) — [`docs/features/opencode-worker-launch-models.md`](../features/opencode-worker-launch-models.md)
  - fix(orchestration): aceptar model ids anidados tipo `provider/model` en OpenCode
  - docs(orchestration): documentar la restricción `--effort` de OpenCode en worker-start
- feat(orchestration): soporte de launch models para workers Pi, compartiendo ladder de thinking y parser de tabla — [`docs/features/pi-worker-launch-models.md`](../features/pi-worker-launch-models.md)
- fix(runtime): enrutar worktrees con catálogo huérfano a su dueño de sesión duradero único (#11803) — [`docs/fixes/runtime-stale-session-owner.md`](../fixes/runtime-stale-session-owner.md)
- fix(daemon): negarse a servir cuando el path del socket excede el budget `sun_path` (#17840) — [`docs/fixes/daemon-socket-path-budget.md`](../fixes/daemon-socket-path-budget.md)
- fix(build): mantener node-pty en el suelo glibc 2.31 cuando `cfset*speed` pasan a ser símbolos reales — [`docs/fixes/node-pty-glibc-2-42-cfset.md`](../fixes/node-pty-glibc-2-42-cfset.md) (+ hash del patch en lockfile)

## Cambios de upstream

Ninguno adicional: la base ya era `upstream/main` del día. Los fixes de worker launch models de OpenCode/Pi nacen de las ramas `feat/opencode-worker-model-selection` y `feat/pi-worker-model-selection` (PRs a upstream finalmente no aceptados, origen del fork).

## Incidencias

- El fix de node-pty/glibc obligó a regenerar el hash del patch en el lockfile durante el propio build (commits `7fd27cc2d6` + `152fe21012`).
- Instalación limpia; sin incidencias.
